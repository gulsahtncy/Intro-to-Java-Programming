package chapter07.exercises;

public class Exercise19 {

	// Each of the following code snippets are incorrect.

	/*
	 * public static void print(String... strings, double... numbers) The problem
	 * with above code is that it is only allowed to have a single variable-length
	 * parameter.
	 * 
	 * public static void print(double... numbers, String name) The problem with the
	 * above is that a variable-length parameter is only allowed to be placed last
	 * int the parameter list.
	 * 
	 * public static double... print(double d1, double d2) The problem with the
	 * above code is that it is not allowed to return a variable-length type from a
	 * method.
	 */

}
